[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-ai-project"
version = "0.1.0"
description = "AI project for learning Python modules and packages"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/my_ai_project"]